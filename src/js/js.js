"use strict";
$(document).ready(() => {

    const cart = new Cart();
    cart.renderProdList();
    // inits Gallery
    // const gallery = new Gallery({
    //     carouselContent: $(".new-arr-slider-gallery")
    // });
    // gallery.init(".nav--left", ".nav--right");

    // inits jCarousel
/*     $('.jcarousel').jcarousel({
        wrap: 'circular'
    });

    $('.jcarousel-prev').click(function () {
        $('.jcarousel').jcarousel('scroll', '-=1');
    });

    $('.jcarousel-next').click(function () {
        $('.jcarousel').jcarousel('scroll', '+=1');
    });
 */
});